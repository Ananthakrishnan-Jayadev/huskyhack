import type { Howl } from 'howler';

export const arcDraw = null;
export const reveal = null;
export const silenceTone = null;

export function playSound(_sound: Howl | null) {
  // Audio is disabled for this build.
}
